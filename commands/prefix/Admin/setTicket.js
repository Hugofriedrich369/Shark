module.exports = {
    config: {
    name: "setTicket",
    description: "Envia a mensagem de ticket",
    usage: "setTicket [chat]"
    },
    permissions: ['Administrator'],
    owner: false,
    run: async (client, message, args, prefix, config, db) => {
        
    }
}